# Runtime (the Lambda and the local server run the same FastAPI app)
fastapi==0.141.1
mangum==0.22.0
uvicorn==0.52.4
boto3==1.43.83
pydantic==2.12.5
httpx==0.28.1
mcp==1.29.1                # the Alexa+ MCP server mounted at /mcp

# Tests
pytest==9.1.1
moto[dynamodb]>=5,<6
websockets==17.1           # scripts/obs_director.py talks to OBS
pytest-asyncio==1.4.0      # the fake OBS server in tests/test_obs_client.py

# Test images for the label reader
pillow>=11
python-multipart==0.0.32   # form posts on /oauth/approve and /oauth/token
